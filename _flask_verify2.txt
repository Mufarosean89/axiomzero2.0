=== Flask API Response ===
/- Auto-generated by Axiom Zero from module: web_module -/

import Mathlib

def floor_div (a : ℤ) (b : ℤ) : ℤ :=
  (Int.fdiv a b)

theorem floor_div_correct
  (a : ℤ)
  (b : ℤ)
  (hpre : b > 0)
  : (((floor_div a b * b) ≤ a) ∧ (a < ((floor_div a b + 1) * b))) :=
  by
    unfold floor_div; constructor
    · have h := Int.fdiv_mul_add_fmod a b
      have hfmod_nonneg : 0 ≤ Int.fmod a b :=
        by
          have h_eq : Int.fmod a b = Int.emod a b :=
            Int.fmod_eq_emod_of_nonneg (le_of_lt hpre)
          rw [h_eq]
          exact Int.emod_nonneg a (ne_of_gt hpre)
      nlinarith
    · exact Int.lt_fdiv_add_one_mul_self a hpre

Has (hpre : b > 0) in signature: True
Has Int.fdiv: True
